import { MenuHighlightDirective } from './menu-highlight.directive';

describe('MenuHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
